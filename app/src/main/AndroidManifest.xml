<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.faro.celula">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permissão android.permission.INTERNET"/>

    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />


    <application
            android:name=".Configuracao"
            android:allowBackup="true"
            android:icon="@mipmap/ic_celula_quadrado"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_celula_redondo"
            android:supportsRtl="true"

            android:theme="@style/Theme.AppCompat.Light">

        <activity  android:name=".AdicionaDadosActivity"/>
        <activity  android:name=".EditarActivity"/>

        <activity android:screenOrientation="portrait" android:name=".RelatorioActivity"/>

        <activity android:name=".ComoUsarActivity"/>
        <activity android:name=".MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
            <action android:name="android.intent.action.VIEW" />
        </intent-filter>
        </activity>
        <activity android:name=".SobreActivity"/>


    </application>

</manifest>